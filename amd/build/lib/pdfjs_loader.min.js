/**
 * PDF.js loader for the unified grader.
 *
 * Loads PDF.js via native dynamic import from the thirdparty directory.
 * Uses new Function() to preserve the import() call from Rollup transformation,
 * which would otherwise convert it to a RequireJS require() that cannot load ES modules.
 *
 * @module     local_unifiedgrader/lib/pdfjs_loader
 * @copyright  2025 SATS
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_unifiedgrader/lib/pdfjs_loader",[],(function(){let pdfjsLib=null,loadPromise=null;const nativeImport=new Function("url","return import(url)");return{load:function(){if(pdfjsLib)return Promise.resolve(pdfjsLib);if(loadPromise)return loadPromise;const base=M.cfg.wwwroot+"/local/unifiedgrader/thirdparty/pdfjs";return loadPromise=nativeImport(base+"/pdf.min.js").then((function(lib){return pdfjsLib=lib,pdfjsLib.GlobalWorkerOptions.workerSrc=base+"/pdf.worker.min.js",pdfjsLib})),loadPromise}}}));

//# sourceMappingURL=pdfjs_loader.min.js.map